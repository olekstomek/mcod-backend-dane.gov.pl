{% load i18n %}
---
tags:
  - API Endpoints
summary: Returns list of articles
description: |
  {% spaceless %}
      <p>Gives the ability to browse, filter and search articles.</p>
      {% include "docs/articles/obj_description.html" %}
  {% endspaceless %}
parameters:
  schema: ArticleApiSearchRequest
responses:
  200:
    description: valid response in jsonapi format
    content:
      application/vnd.api+json:
        schema:
          $ref: '#/components/schemas/Articles'

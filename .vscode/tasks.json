{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run flake8",
      "type": "shell",
      "command": "${config:python.pythonPath}",
      "args": ["-m", "flake8", "mcod"]
    },
    {
      "label": "compile translations",
      "type": "shell",
      "command": "${config:python.pythonPath}",
      "args": ["manage.py", "compilemessages"]
    }
  ]
}
